/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn-icons-png.flaticon.com'], // Add your external image domain here
    },
  };
  
  export default nextConfig;
  